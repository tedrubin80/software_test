{
  "name": "testlab-simple",
  "version": "1.0.0",
  "description": "FILE LOCATION: /package.json (root directory) - Minimal package for debugging",
  "main": "simple-server.js",
  "scripts": {
    "start": "node simple-server.js",
    "test": "node -e \"console.log('Server test passed')\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}